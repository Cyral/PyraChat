<controls:MetroWindow x:Class="Pyratron.PyraChat.UI.Views.MainView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
                      mc:Ignorable="d"
                      Title="PyraChat IRC"
                      TitleCaps="False"
                      Width="1024"
                      Height="512"
                      Style="{DynamicResource MainWindowStyle}">
    <StackPanel>
        <TextBox Margin="16" IsReadOnly="True" HorizontalAlignment="Stretch" Height="402" VerticalAlignment="Stretch"
                 VerticalScrollBarVisibility="Auto" Text="{Binding Text}" />
        <TextBox Margin="16,0,16,16" VerticalAlignment="Stretch"
                 VerticalScrollBarVisibility="Auto" Text="{Binding Chat}">
            <TextBox.InputBindings>
                <KeyBinding Key="Enter" Command="{Binding EnterCommand}"/>
            </TextBox.InputBindings>
        </TextBox>
    </StackPanel>
</controls:MetroWindow>